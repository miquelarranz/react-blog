import { createUseStyles } from 'react-jss';

export const useAppStyles = createUseStyles({
    container: {
        height: '100vh',
        display: 'flex',
        flexDirection: 'column',
    },
});
